import React from 'react';
import { Navigate } from 'react-router-dom';

const LashesDallasPage: React.FC = () => {
  return <Navigate to="/artists/lashes/dallas-tx" replace />;
};

export default LashesDallasPage;