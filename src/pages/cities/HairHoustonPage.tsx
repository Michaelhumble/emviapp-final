import React from 'react';
import { Navigate } from 'react-router-dom';

const HairHoustonPage: React.FC = () => {
  return <Navigate to="/artists/hair/houston-tx" replace />;
};

export default HairHoustonPage;