
// This is now just a re-export of the main hook
import { useProfileCompletion as useProfileCompletionProvider } from '@/hooks/useProfileCompletion';

export const useProfileCompletion = useProfileCompletionProvider;
