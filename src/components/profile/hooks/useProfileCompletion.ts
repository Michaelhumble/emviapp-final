
import { useProfileCompletion as useProfileCompletionProvider } from '@/context/profile';

// Re-export the hook for better organization
export const useProfileCompletion = useProfileCompletionProvider;
