
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const SalonProfileEditor = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Salon Profile Editor</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Salon profile editing functionality will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default SalonProfileEditor;
