
import React from "react";
// Removed all suggestions—dashboard portfolio prompt is redundant per UI guidelines

/**
 * [REMOVED] This component is no longer rendered for artists directly.
 * Portfolio engagement already exists contextually within the dashboard.
 */

export default function SmartSuggestions() {
  return null; // Return nothing: no suggestion block rendered
}
