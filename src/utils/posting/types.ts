
// Define basic types used across posting utilities
export type PostType = 'job' | 'salon' | 'booth' | 'supply';

// Re-export PricingOptions from the proper types file
export { PricingOptions } from '@/types/pricing';
